{
  "scripts": {
    "spago": "spago",
    "format": "purty src/Main.purs --write",
    "lci": "spago test --watch --clear-screen",
    "start": "spago run -a --create -a polarbear"
  },
  "devDependencies": {
    "purescript": "^0.14.0",
    "purty": "^7.0.0",
    "spago": "^0.19.2"
  }
}
